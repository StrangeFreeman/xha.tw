---
import { cmsPage } from '@/cms-config'
import { isPreview } from '@/utils/preview'

import PageLayout from '@/layouts/CommonPage.astro'
import PageBlocks from '@/components/blocks/PageBlocks.astro'
import { getBlockHeadings } from '@/utils/page-blocks'

if (!cmsPage.projects.enabled && !isPreview) return Astro.redirect('/404')
const headings = getBlockHeadings(cmsPage.projects.blocks)
---

<PageLayout
  title={cmsPage.projects.title}
  description={cmsPage.projects.description}
  {headings}
  view={cmsPage.projects.view}
  comment={cmsPage.projects.comment}
>
  <PageBlocks blocks={cmsPage.projects.blocks} />
</PageLayout>
